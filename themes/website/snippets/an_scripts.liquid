{% comment %}
  AN Theme Modern Script Loading
  Conditional, performant JavaScript loading
{% endcomment %}

<!-- Core AN Theme Scripts (lightweight ~20KB) -->
<!-- Using kajabi_asset_url for proper CORS headers and MIME types -->
<script src="{{ 'an-core.js' | kajabi_asset_url }}" defer></script>
<script src="{{ 'an-modules.js' | kajabi_asset_url }}" defer></script>

<!-- Enhanced interactions -->
{% include 'button_states' %}
{% include 'micro_interactions' %}
{% include 'dynamic_backgrounds' %}

{% comment %} jQuery removed - saves 90KB per page load! {% endcomment %}

{% comment %}
  AOS - DISABLED DUE TO CSP VIOLATIONS
  The AOS library uses 'new Function' which violates Kajabi's Content Security Policy.
  Animations now handled by CSS classes directly (animate-fade-up, animate-fade-in, etc.)
{% endcomment %}

{% comment %}
  Moment.js - DISABLED DUE TO CSP VIOLATIONS
  Moment.js uses 'new Function' which violates Kajabi's Content Security Policy.
  Countdown functionality now uses native Date API in an-core.js
{% endcomment %}

{% comment %} Slick Carousel removed - no longer needed! {% endcomment %}

{% comment %} Wistia - Only load API if Wistia videos exist {% endcomment %}
{% if page.content contains 'wistia' %}
  <script>
    // Wistia API will auto-load when needed
    window._wq = window._wq || [];
  </script>
{% endif %}

{% comment %} Performance monitoring (optional) {% endcomment %}
{% if settings.enable_performance_monitoring %}
  <script>
    // Log performance metrics
    window.addEventListener('load', () => {
      if (window.performance && window.performance.timing) {
        const timing = window.performance.timing;
        const loadTime = timing.loadEventEnd - timing.navigationStart;
        console.log(`Page load time: ${loadTime}ms`);
        
        // Send to analytics if needed
        if (window.gtag) {
          gtag('event', 'timing_complete', {
            'name': 'load',
            'value': loadTime
          });
        }
      }
    });
  </script>
{% endif %}

{% comment %} Custom JS from theme settings {% endcomment %}
{% include "global_custom_js" %}