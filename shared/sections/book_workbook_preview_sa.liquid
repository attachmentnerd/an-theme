<section id="{{ section.settings.section_id | default: 'book-workbook-preview-sa' }}-{{ section.id }}" class="sa-workbook-preview py-5" style="background: linear-gradient(180deg, #FFFFFF 0%, var(--c-brand-100) 100%);">
  <div class="container">
    <div class="text-center mb-5 animate-fade-up">
      <h2 class="mb-3 text-h1 text-ink-900 font-medium" kjb-settings-id="{{ 'heading' | settings_id: section: section }}">
        {{ section.settings.heading | default: 'A Guided Journey, Not Just a Book' }}
      </h2>
      <p class="text-body-lg text-ink-700 mx-auto" style="max-width: 600px;" kjb-settings-id="{{ 'subheading' | settings_id: section: section }}">
        {{ section.settings.subheading | default: 'This informative guided journal combines education with self-discovery' }}
      </p>
    </div>
    
    <div class="row g-5 align-items-center">
      <div class="col-lg-6">
        <div class="animate-fade-up">
          {% if section.settings.left_image != blank %}
            <div class="position-relative">
              <div kjb-settings-id="{{ 'left_image' | settings_id: section: section }}">
                {% render 'responsive-image',
                  image: section.settings.left_image,
                  alt: section.settings.left_image_alt | default: 'Workbook preview',
                  sizes: '(max-width: 767px) 100vw, (max-width: 991px) 50vw, 600px',
                  aspect_ratio: '4/3',
                  widths: '400,600,800,1200',
                  placeholder_type: 'feature',
                  blur_up: true,
                  class: 'img-fluid rounded-lg shadow-lg'
                %}
              </div>
              {% if section.settings.show_left_badge and section.settings.left_badge_text != blank %}
                <div class="position-absolute animate-float bg-white px-4 py-3 rounded-pill shadow-lg" style="top: -20px; right: -20px;" kjb-settings-id="{{ 'left_badge_text' | settings_id: section: section }}">
                  <span class="text-sm font-semibold text-ink-800">{{ section.settings.left_badge_text }}</span>
                </div>
              {% endif %}
            </div>
          {% else %}
            <!-- Preview mockup (fallback when no image set) -->
            <div class="position-relative">
              <!-- Browser-like frame -->
              <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                <!-- Browser top bar -->
                <div class="d-flex align-items-center gap-2 px-3 py-2" style="background: #F5F5F5;">
                  <div class="rounded-circle" style="width: 12px; height: 12px; background: #FF5F57;"></div>
                  <div class="rounded-circle" style="width: 12px; height: 12px; background: #FFBD2E;"></div>
                  <div class="rounded-circle" style="width: 12px; height: 12px; background: #28CA42;"></div>
                </div>
                
                <!-- Preview content -->
                <div class="p-5">
                  <h3 class="mb-3 text-2xl text-ink-900 font-medium" kjb-settings-id="{{ 'preview_title' | settings_id: section: section }}">
                    {{ section.settings.preview_title | default: 'Sample Prompt #47' }}
                  </h3>
                  <div class="p-4 rounded mb-4" style="background: var(--c-brand-100);">
                    <p class="mb-0 text-ink-800" style="font-style: italic;" kjb-settings-id="{{ 'preview_quote' | settings_id: section: section }}">
                      "{{ section.settings.preview_quote | default: 'Think about a time when you felt truly safe and connected in a relationship. What made that moment special? How did your body feel? What thoughts went through your mind?' }}"
                    </p>
                  </div>
                  <div class="ps-3" style="border-left: 3px solid var(--c-accent-teal);">
                    <p class="text-ink-600" style="line-height: 2;">
                      <span style="opacity: 0.4;" kjb-settings-id="{{ 'preview_placeholder' | settings_id: section: section }}">{{ section.settings.preview_placeholder | default: 'Write your reflection here...' }}</span>
                    </p>
                  </div>
                </div>
              </div>
              
              <!-- Floating elements -->
              {% if section.settings.show_left_badge %}
                <div class="position-absolute animate-float bg-white px-4 py-3 rounded-pill shadow-lg" style="top: -20px; right: -20px;">
                  <span class="text-sm font-semibold text-ink-800">{{ section.settings.left_badge_text | default: 'Interactive' }}</span>
                </div>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
      
      <div class="col-lg-6">
        <div class="animate-fade-up" style="animation-delay: 0.1s;">
          <h3 class="mb-4 text-h2 text-ink-900 font-medium" kjb-settings-id="{{ 'content_heading' | settings_id: section: section }}">
            {{ section.settings.content_heading | default: 'How This Workbook Works' }}
          </h3>
          
          <!-- Process steps -->
          {% for block in section.blocks %}
            {% if block.type == 'step' %}
              <div class="d-flex align-items-start mb-4 animate-fade-up" style="animation-delay: {{ forloop.index | times: 0.1 | plus: 0.2 }}s;">
                <div class="flex-shrink-0 me-3">
                  <div class="d-flex align-items-center justify-content-center rounded-circle text-white font-semibold" style="width: 40px; height: 40px; background: var(--g-brand);">
                    {{ forloop.index }}
                  </div>
                </div>
                <div>
                  <h4 class="mb-2 text-body-lg text-ink-900 font-medium">
                    {{ block.settings.title }}
                  </h4>
                  <p class="text-ink-600" style="line-height: 1.6;">
                    {{ block.settings.description }}
                  </p>
                </div>
              </div>
            {% endif %}
          {% endfor %}
          
          <!-- Nerd Alert callout -->
          {% if section.settings.show_nerd_alert %}
            <div class="mt-4 p-4 rounded-lg" style="background: var(--c-accent-lemon); background-opacity: 0.2; border: 2px dashed var(--c-accent-lemon);">
              <div class="d-flex align-items-start">
                <div class="me-3">
                  <span class="text-2xl">🤓</span>
                </div>
                <div>
                  <p class="mb-0 font-semibold text-ink-900" kjb-settings-id="{{ 'nerd_alert_heading' | settings_id: section: section }}">{{ section.settings.nerd_alert_heading | default: 'Nerd Alert!' }}</p>
                  <p class="mb-0 mt-1 text-sm text-ink-700" kjb-settings-id="{{ 'nerd_alert_text' | settings_id: section: section }}">
                    {{ section.settings.nerd_alert_text | default: 'Throughout the book, I share "Nerd Alerts" to help you understand the rich data behind attachment research and how it applies to your life.' }}
                  </p>
                </div>
              </div>
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Book - Workbook Preview SA",
  "elements": [
    {
    "type": "header",
      "content": "General Settings"
    },
    {
      "type": "text",
      "id": "section_id",
      "label": "Section ID",
      "default": "book-workbook-preview-sa",
      "info": "Custom ID for internal linking (e.g., #book-workbook-preview-sa)"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "A Guided Journey, Not Just a Book"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading"
    },
    {
      "type": "text",
      "id": "content_heading",
      "label": "Content Heading",
      "default": "How This Workbook Works"
    },
    {
      "type": "header",
      "content": "Left Side Image"
    },
    { "type": "image_picker", "id": "left_image", "label": "Left Image (optional)" },
    { "type": "text", "id": "left_image_alt", "label": "Left image alt text" },
    { "type": "checkbox", "id": "show_left_badge", "label": "Show floating badge on left", "default": true },
    { "type": "text", "id": "left_badge_text", "label": "Left badge text", "default": "Interactive" },
    {
      "type": "header",
      "content": "Mockup Text (fallback when no image)"
    },
    { "type": "text", "id": "preview_title", "label": "Mockup title", "default": "Sample Prompt #47" },
    { "type": "textarea", "id": "preview_quote", "label": "Mockup quoted prompt", "default": "Think about a time when you felt truly safe and connected in a relationship. What made that moment special? How did your body feel? What thoughts went through your mind?" },
    { "type": "text", "id": "preview_placeholder", "label": "Mockup placeholder", "default": "Write your reflection here..." },
    {
      "type": "header",
      "content": "Callout"
    },
    { "type": "checkbox", "id": "show_nerd_alert", "label": "Show Nerd Alert callout", "default": true },
    { "type": "text", "id": "nerd_alert_heading", "label": "Nerd Alert heading", "default": "Nerd Alert!" },
    {
      "type": "textarea",
      "id": "nerd_alert_text",
      "label": "Nerd Alert Text"
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Process Step",
      "elements": [
        {
          "type": "text",
          "id": "title",
          "label": "Step Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Step Description"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Book - Workbook Preview SA",
      "category": "Content",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "title": "Learn & Reflect",
            "description": "Discover attachment theory concepts through clear explanations, then use the 140 prompts to explore your own patterns and experiences."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Process & Grieve",
            "description": "Allow yourself to feel and process past experiences. The research shows that reflection and grieving lead to freedom in present relationships."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Practice & Grow",
            "description": "Apply practical relational skills from the final section to build secure, lasting connections in your life."
          }
        }
      ]
    }
  ]
}
{% endschema %}
